const PricingRuleType = {
    XForY: 1, // X for Y rule, e.g. 3 for 2 deal
    BulkPrice: 2, // Bulk price rule
    FreeIf: 3, // Free item in case of another purchase
}

module.exports = PricingRuleType;